# mindcall-health-intent-classifier
Machine learning pipeline that classifies patient queries into 20 health-tracking intents (sleep, heart rate, steps, etc.) using TF-IDF + SVM/Logistic Regression/Random Forest, with SHAP/LIME explainability and embedding-based clustering on the MindCall dataset.
